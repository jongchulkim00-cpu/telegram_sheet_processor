services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: telegram-sheet-cloudflared
    command: tunnel --no-autoupdate run --token ${CLOUDFLARED_TOKEN}
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      - stock-api
